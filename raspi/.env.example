# Copy this file to .env and fill in real values. Never commit the real .env.

### Global ###
TZ=Europe/Madrid
DOMAIN_BASE=home.arpa
# Used by scripts/setup-firewall.sh to scope SSH/HTTP(S) access to your LAN
LOCAL_SUBNET=192.168.1.0/24
# This box's own LAN IP. Auto-detected and kept up to date by
# scripts/detect-lan-ip.sh, which scripts/setup.sh runs on every invocation -
# you shouldn't normally need to touch this by hand. Only used as a fallback
# default if you run `docker compose up` directly without going through
# setup.sh first (e.g. on a machine with no internet route to test against).
# Used to auto-populate Pi-hole's Local DNS Records for every *.home.arpa
# hostname, so you don't have to add them by hand in the admin UI.
SERVER_LAN_IP=192.168.1.32
# Optional: your Tailscale IP for this box, added as a cert SAN so HTTPS
# still validates when you connect over the raw Tailscale IP. Leave blank to skip.
TAILSCALE_IP=

### Traefik ###
# Generate with: htpasswd -nbB admin 'yourpassword'   (needs apache2-utils)
# Protects the Traefik dashboard and Prometheus UI. Escape $ as $$ in .env files.
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$replace.with.a.real.bcrypt.hash
MONITORING_AUTH=admin:$$2y$$05$$replace.with.a.real.bcrypt.hash

### Vaultwarden ###
PG_USER=vaultwarden
PG_PASSWORD=change-me-strong-password
VAULTWARDEN_ADMIN_TOKEN=change-me-generate-with-openssl-rand-base64-48
VAULTWARDEN_JWT_SECRET=change-me-generate-with-openssl-rand-base64-48

### OctoPrint ###
OCTOPRINT_USER=admin
OCTOPRINT_PASS=change-me

### Pi-hole ###
PIHOLE_PASSWORD=change-me
# Upstream DNS resolvers Pi-hole forwards non-blocked queries to
PIHOLE_UPSTREAM_DNS_1=1.1.1.1
PIHOLE_UPSTREAM_DNS_2=1.0.0.1
# DHCP range Pi-hole will serve (only if you disable your router's DHCP)
PIHOLE_DHCP_START=192.168.1.100
PIHOLE_DHCP_END=192.168.1.250
PIHOLE_DHCP_ROUTER=192.168.1.1
PIHOLE_DHCP_NETMASK=255.255.255.0
# Pi-hole runs with network_mode: host, so it's reachable at the Pi's own
# LAN IP - same as everything else behind Traefik. No separate IP needed.

### Nextcloud ###
NEXTCLOUD_DB_PASSWORD=change-me
NEXTCLOUD_DB_ROOT_PASSWORD=change-me
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=change-me
COLLABORA_ADMIN_USER=admin
COLLABORA_ADMIN_PASSWORD=change-me

### Scheduler Discord bot ###
GITHUB_USERNAME=your-github-username
GITHUB_TOKEN=ghp_xxxxxxxxxxxx
BOT_TOKEN=your-discord-bot-token
BOT_DATABASE_URL=sqlite:////app/db/scheduler.db
BOT_ENVIRONMENT=production
BOT_LOG_LEVEL=INFO

### Tailscale ###
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx

### Grafana ###
GRAFANA_ADMIN_PASSWORD=change-me
